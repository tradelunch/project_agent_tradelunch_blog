# 📁 파일 구조 및 번호 체계

## 번호 체계 설명

파일명에 번호를 추가하여 읽기 순서와 의존성을 명확히 했습니다.

### 번호 범위

- **00-09**: 기본 설정 및 인프라
- **10-19**: 사용자 인터페이스
- **20-89**: 기능 모듈 (미래 확장용)
- **90-99**: 테스트 파일

---

## 📂 프로젝트 구조

```
blog-agent/
│
├── 00_config.py                    # 전역 설정
├── 01_schema.py                    # DB 스키마 정의
├── 10_cli_multi_agent.py           # CLI 인터페이스
│
├── 90_test_agents.py               # 기본 에이전트 테스트
├── 91_test_improved_agents.py      # 개선된 기능 테스트
│
├── agents/                          # 에이전트 모듈
│   ├── __init__.py                 # 패키지 초기화
│   │
│   ├── 00_base.py                  # BaseAgent 추상 클래스
│   ├── 01_protocol.py              # 통신 프로토콜
│   │
│   ├── 02_document_scanner_agent.py    # 폴더 스캔 에이전트
│   ├── 03_extracting_agent.py          # 파싱 에이전트
│   ├── 04_uploading_agent.py           # 업로드 에이전트
│   ├── 05_logging_agent.py             # 로깅 에이전트
│   └── 06_project_manager.py           # 오케스트레이터
│
├── docs/                            # 테스트용 문서
│   └── technology/
│       └── ai/
│           └── langchain-guide/
│               ├── langchain-guide.md
│               ├── langchain-guide.png
│               └── ...
│
├── posts/                           # 샘플 포스트
│   └── sample-post.md
│
├── README.md                        # 프로젝트 소개
├── QUICKSTART.md                    # 빠른 시작 가이드
├── IMPROVED_ARCHITECTURE.md         # 개선 사항 문서
└── requirements.txt                 # Python 의존성
```

---

## 📋 파일별 설명

### 00-09: 기본 설정

#### `00_config.py`
- 모든 설정을 한 곳에서 관리
- LLM, AWS, MCP, 파일 경로 등

#### `01_schema.py`
- `ArticleSchema`: Pydantic 데이터 모델
- `get_schema_description()`: LLM용 스키마 설명
- `calculate_reading_time()`: 읽기 시간 계산

---

### 10-19: 사용자 인터페이스

#### `10_cli_multi_agent.py`
- 대화형 CLI
- 명령어: `upload`, `process`, `status`, `agents` 등
- Rich UI (색상, 테이블)

---

### agents/: 에이전트 모듈

#### `00_base.py` - 기반 클래스
모든 에이전트가 상속받는 추상 클래스

**주요 메서드**:
- `execute()`: 추상 메서드 (각 에이전트가 구현)
- `run()`: 실행 래퍼 (상태 관리)
- `_log()`: 로깅

#### `01_protocol.py` - 통신 프로토콜
에이전트 간 메시지 교환 표준

**클래스**:
- `AgentMessage`: 메시지
- `AgentTask`: 작업 정의
- `AgentResponse`: 결과

#### `02_document_scanner_agent.py` - 폴더 스캔
문서 폴더 구조 분석

**기능**:
- 폴더 재귀 스캔
- article 폴더 감지
- 썸네일 식별
- 카테고리 추출

**입력**: `root_path` (예: `./docs`)
**출력**: `articles`, `category_tree`

#### `03_extracting_agent.py` - 파싱 & 추출
마크다운 파싱 및 메타데이터 추출

**기능**:
- frontmatter 파싱
- 이미지 추출
- slug 생성
- **LLM**: 태그 생성 (5-7개)
- **LLM**: 3문장 요약
- 읽기 시간 계산

**입력**: `file_path` 또는 `article_info`
**출력**: 구조화된 article 데이터

#### `04_uploading_agent.py` - 업로드
S3 이미지 업로드 & RDS 저장

**기능**:
- 썸네일 우선 업로드
- 본문 이미지 업로드
- URL 매핑
- 스키마 검증
- DB 저장

**입력**: extracted article data
**출력**: `article_id`, URLs

#### `05_logging_agent.py` - 로깅
통일된 로그 및 터미널 출력

**기능**:
- 포맷팅 (Rich)
- 진행 상태 표시
- 결과 요약
- 에러 강조

#### `06_project_manager.py` - 오케스트레이터
전체 워크플로우 관리 (LangGraph)

**역할**:
- 사용자 명령 분석 (Qwen3)
- 작업 계획 수립
- 에이전트 호출 순서 결정
- 데이터 흐름 관리

**워크플로우**:
```
analyze_command → extract → upload → finalize
```

---

### 90-99: 테스트 파일

#### `90_test_agents.py`
기본 에이전트 기능 테스트
- Ollama 없이도 대부분 테스트 가능

#### `91_test_improved_agents.py`
개선된 기능 테스트
- DocumentScanner
- 태그/요약 생성
- 썸네일 처리
- 스키마 검증

---

## 🔄 실행 순서

### 1. 기본 테스트
```bash
python 91_test_improved_agents.py
```

### 2. CLI 실행
```bash
# Terminal 1: Ollama
ollama serve

# Terminal 2: CLI
python 10_cli_multi_agent.py
```

---

## 📖 읽기 순서 (학습용)

이 프로젝트를 처음 보는 사람을 위한 읽기 순서:

1. **`README.md`** - 프로젝트 개요
2. **`00_config.py`** - 설정 이해
3. **`01_schema.py`** - 데이터 구조 이해
4. **`agents/00_base.py`** - 에이전트 기본 개념
5. **`agents/01_protocol.py`** - 통신 방식
6. **`agents/02_document_scanner_agent.py`** - 첫 번째 에이전트
7. **`agents/03_extracting_agent.py`** - 핵심 에이전트
8. **`agents/04_uploading_agent.py`** - 외부 통신
9. **`agents/05_logging_agent.py`** - UI/UX
10. **`agents/06_project_manager.py`** - 전체 통합
11. **`91_test_improved_agents.py`** - 테스트 실행
12. **`10_cli_multi_agent.py`** - 최종 인터페이스

---

## 🎯 의존성 그래프

```
00_config.py (설정)
    ↓
01_schema.py (스키마)
    ↓
agents/00_base.py (베이스)
    ↓
agents/01_protocol.py (프로토콜)
    ↓
┌───────────────┬─────────────┬─────────────┐
│               │             │             │
02_document     03_extracting 04_uploading  05_logging
_scanner        _agent        _agent        _agent
    │               │             │             │
    └───────────────┴─────────────┴─────────────┘
                    ↓
            06_project_manager
                    ↓
            10_cli_multi_agent
```

---

## ✨ 번호 체계의 장점

1. **명확한 순서**: 한 눈에 읽는 순서를 알 수 있음
2. **의존성 파악**: 낮은 번호가 높은 번호에 의존됨
3. **확장성**: 중간에 파일 추가 가능 (예: `15_web_ui.py`)
4. **구조화**: 기능별 번호 범위로 정리

---

## 🚀 새 에이전트 추가하기

새로운 에이전트를 추가하려면:

1. **적절한 번호 선택**
   - 07-09: 추가 에이전트 (예: ValidationAgent)
   
2. **파일 생성**
   ```python
   # agents/07_validation_agent.py
   from .00_base import BaseAgent
   
   class ValidationAgent(BaseAgent):
       ...
   ```

3. **`agents/__init__.py`에 추가**
   ```python
   from .07_validation_agent import ValidationAgent
   ```

4. **ProjectManager에 통합**
   ```python
   self.validation_agent = ValidationAgent()
   ```

---

이제 파일명만 봐도 전체 구조를 이해할 수 있습니다! 🎉
